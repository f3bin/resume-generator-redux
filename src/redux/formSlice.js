import {createSlice} from '@reduxjs/toolkit';

const initialState = {
name:"",
}

const formSlice = createSlice({
     name:"form",
     initialState,
     reducers:{

     }
})

export default formSlice.reducer;
const {} =formSlice.actions;